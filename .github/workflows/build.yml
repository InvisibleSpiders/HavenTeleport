name: Build

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  packages: read

jobs:
  build:
    name: Test and Build Jar
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: gradle

      - name: Check out HavenCore
        uses: actions/checkout@v4
        with:
          repository: InvisibleSpiders/HavenCore
          path: .deps/HavenCore

      - name: Publish HavenCore API locally
        run: |
          chmod +x .deps/HavenCore/gradlew
          .deps/HavenCore/gradlew -p .deps/HavenCore :haven-api:publishToMavenLocal --no-daemon

      - name: Check out HavenClaims
        uses: actions/checkout@v4
        with:
          repository: InvisibleSpiders/landclaiming
          path: .deps/landclaiming

      - name: Publish HavenClaims API locally
        run: |
          chmod +x .deps/landclaiming/gradlew
          .deps/landclaiming/gradlew -p .deps/landclaiming :havenclaims-api:publishToMavenLocal --no-daemon

      - name: Build
        run: |
          chmod +x ./gradlew
          ./gradlew clean test build --no-daemon
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: TeleportLocations
          path: build/libs/TeleportLocations-*.jar
          if-no-files-found: error
          retention-days: 14
