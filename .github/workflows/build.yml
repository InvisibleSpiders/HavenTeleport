name: Build

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  packages: read

jobs:
  build:
    name: Test and Build Jar
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: gradle

      - name: Check out HavenClaims
        uses: actions/checkout@v4
        with:
          repository: InvisibleSpiders/landclaiming
          path: .deps/landclaiming

      - name: Publish HavenClaims API locally
        run: |
          chmod +x .deps/landclaiming/gradlew
          .deps/landclaiming/gradlew -p .deps/landclaiming :havenclaims-api:publishToMavenLocal --no-daemon

      - name: Build
        run: |
          chmod +x ./gradlew
          ./gradlew clean test build --no-daemon

      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: TeleportLocations
          path: build/libs/TeleportLocations-*.jar
          if-no-files-found: error
          retention-days: 14
